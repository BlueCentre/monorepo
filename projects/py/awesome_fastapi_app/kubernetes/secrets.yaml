apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: awesome-fastapi-app
  labels:
    app.kubernetes.io/name: awesome-fastapi-app
    app.kubernetes.io/managed-by: copier
    app.kubernetes.io/component: secret
    app.kubernetes.io/part-of: awesome_fastapi_app
type: Opaque
# We keep both stringData (developer-friendly) and data (base64) for clarity.
# Kubernetes will merge them; data takes precedence if keys overlap.
stringData:
  # Plaintext value provided during template generation (or auto-generated if left blank)
  secret-key: 0pensaysme
# Base64 encoded version (auto-derived) – shown for transparency & CI diffs
# NOTE: Do not edit manually; re-run copier to change.
data:
  secret-key: MHBlbnNheXNtZQ==
